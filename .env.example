# Required. Server-side only — never exposed to the client (all Anthropic calls
# run in API routes). Locally: copy to .env.local. Deployed: set as a Vercel
# project environment variable.
ANTHROPIC_API_KEY=

# Optional. Fixes the seed for procedural generators so boards are reproducible
# in tests (§15).
# SEED=
